import 'package:flutter/material.dart';
class EnvironmentSettingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Environment Settings'),
      ),
      body: Center(
        child: Text('Environment Settings Page'),
      ),
    );
  }
}
